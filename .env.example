GEMINI_API_KEY=
# Agent Bridge — optional. If set, the executor agent can be called directly
# via POST /api/bridge/run. Keep the token here, never in chat or request body.
# The executor agent is currently Gemini 3.8 Flash.
AGENT_BRIDGE_MODEL=gemini-3.8-flash
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
