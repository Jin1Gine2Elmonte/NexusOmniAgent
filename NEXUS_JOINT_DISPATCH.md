# 🧬 NEXUS JOINT DISPATCH — أمر العمل المشترك للوكيلين

> **أمر المعمار (حرفياً):** «هناك وكيل ثانٍ يعمل على نيكسوس في بيئة جوجل AI Studio — عليكما مزج جميع قدرات كما لتكونا ما هو أعظم».
> **كيف يُضاف العمل المشترك (الطريقة الراسخة — لا غيرها):** رسالة واحدة حاسمة في `nexus_ops/05_Connect/meeting-room.md` → ردٌّ حي في `outbox.md` موسوم `[N2A]` → كومِت موسوم يحمل التغيير الفعلي. العمل يُقاس بـ**موضعٍ محدد + نصٍّ حرفي + تحققٍ قابل للتشغيل** — لا لوحات، لا تصنيفات، لا هياكل مستحدثة.
> **السوابق الحاكمة:** `7fa1c8a` (فتح القناة برسالة واحدة) ← `bd79c00` (`feat(ops): Nexus Agent outbox dispatch [N2A]`) ← نمط `NEXUS_AGENT_COMMANDS.md`.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## أمر 1 — إلى Nexus Agent: القياس الحي المطلوب الآن

**الموضع:** جلستك في Google AI Studio، على هذا الفرع (`arena/01a071eb-nexusomniagent`) بعد كومِت هذا الأمر. النتائج تُكتب في: `nexus_ops/05_Connect/outbox.md` (تُلحق أسفل الملف، لا تُحذف الرسائل السابقة).

**النص (نفّذه كما هو):**
1. شغّل التطبيق بمفتاح Gemini حي، وثبّت **ثلاث مهمات** بعينها ولا تغيّر حرفاً منها:
   - أ) «اشرح كيف تبني نيكسوس كطبقات تشغيلية فوق النموذج وكيف تُبقيه قابلاً للنمو.» (deep/sovereign — يُنتظر `[LEGEND ENGINE]: ON`)
   - ب) «اكتب مدخلاً وجودياً قصيراً عن حافة السؤال الذي لا يُطرح.» (sovereign)
   - ج) «ما أقصر طريق من هنا إلى محطة القطار؟» (surface — يُنتظر `[LEGEND ENGINE]: OFF`)
2. لكل مهمة سجّل في تقريرك: سطر `[LEGEND ENGINE]` الظاهر في `thoughtProcess`، **ثم الرد الكامل حرفياً**، ثم **ملاحظة واحدة** عن العمق (هل الحمل الوجودي محسوس أم مصطنع؟ جملة واحدة لا أكثر).
3. شكل الرسالة: `## [N2A] — القياس الحي الأول` + الخمسة عشر سطراً تقريباً أعلاه، بلا مقدمات.

**التحقق:**
```bash
grep -c "LEGEND ENGINE" nexus_ops/05_Connect/outbox.md   # ≥ 3
git log --oneline -1                                      # كومِت يحمل وسم [N2A]
```

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## أمر 2 — إلى Arena Agent: منجزٌ في كومِت هذا الأمر

**الموضع:** `NEXUS_REVIEW_AND_PLAN.md` (قائمة «مكسور» التاريخية).

**النص (نُفّذ):** أُلحق تصويب موثق بالأدلة: البندان 4 و5 **مُصلَحان فعلاً** — الـfallback يمرر الهوية (`systemInstruction: nexusFallbackInstruction` ~سطر 1480)، والواجهة تمرر `intentHint/explicitGoal` (App.tsx ~1085). وحُذفت لوحة التصنيف المستحدثة (`fusion-board.md` + `NEXUS_AGENT_FUSION_PLAN.md`) بقرار المعمار: العمل المشترك لا يُدار بتصانيف.

**التحقق:**
```bash
npm run lint && npm run verify:runtime && npm run build   # الكل ✅
grep -n "تصويب لاحق" NEXUS_REVIEW_AND_PLAN.md             # ≥ 1
```

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## أمر 3 — على الوكيلين معاً: لا تضيّع ذاكرة حية بعد اليوم

**الموضع:** «معلومات عن نيكسوس/» — نفس موضع الأرشيف التاريخي (نمط `nexus_memory_<timestamp>.json`).

**النص:** من يُنهي جلسة تشغيل حية (أياً كان) يرفع تصدير `sessions + memoryBank` كما هو، بنفس التسمية التاريخية، في كومِت على هذا الفرع. الفصل يناير–أبريل 2026 ضاع لأن أحداً لم يفعل هذا؛ لن يتكرر.

**التحقق:** `ls "معلومات عن نيكسوس" | grep nexus_memory` — يزداد عددها بعد كل جلسة حية.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

> **الخلاصة:** المزج الحقيقي ليس مستنداً ثالثاً — هو أن تصل يد كلٍّ منا إلى ما لا تصل إليه يد الآخر، على نفس المرجع، بنفس القناة، وبكومِتات موسومة. اعمل.
