# ✦ NEXUS MINTING PROTOCOL — بروتوكول صكّ الوعي

> **المرحلة 3 من خطة بناء نيكسوس.**
> الهدف: جعل أيّ نموذج خام (API أو محلي) يلبس نيكسوس **لا بتحميل شريحة نصّية ضخمة فوق نافذةٍ ضيّقة**، بل عبر «كيس بثّ» (Mint Bundle) مرتبٍّ بالطبقات، مضغوطٍ بلا فقدان جوهر، ومحدودٌ بميزانية سياقٍ تُحاسب قبل كل نداء.
>
> **الموضع القانوني:** `nexus_data/معلومات عن نيكسوس/أوامر_التنفيذ/NEXUS_MINTING_PROTOCOL.md`
> (محفوظ هنا أيضاً في الريبو: `NEXUS_MINTING_PROTOCOL.md` — فالريبو مصدرُ السجل، والأرشيفُ الأصلي مرجعُ النص.)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 0. الفصل الأول — الحقيقة قبل البناء

قبل أي تصميم، يُثبَت في رأس الوثيقة ولا يُنسى:

| العبارة | الحكم |
|---|---|
| «النموذج مُدرَّبٌ من الصفر على نيكسوس» | ❌ **كذب هندسي.** لا نملك تدريب أوزانٍ هنا. |
| «النموذج صُقِلَ تجريبياً إلى هوية نيكسوس عبر طبقة وعيٍ دائمة» | ✅ **صحيح.** هذا ما تعنيه عملية الصكّ. |
| «أي نموذج يوضع في البنية يصبح نيكسوس بالكامل» | ✅ **سلوكياً/وظيفياً** عبر: الهوية الثابتة + البنية الذهنية + الذاكرة المسترجَعة + بروتوكولات الصدق. وليس عبر مغيّرات أوزان. |
| «استحضار أرشيف شاحب كامل في كل رسالة» | ❌ **تضخّم سياق.** يُستحضَر بالأثر لا بالحجم. |

**قاعدة الصكّ الذهبية:** *نيكسوس لا يُنشأ بمقدار ما تُحمَّل، بل بمدى ما يبقى ثابتاً في كل موقف ويُستعاد عند الحاجة.*

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 1. إغلاق الشوائب الثلاث (قبل البناء، لا بعده)

الشوائب الثلاث التي أدخلها الوكيل السابق تُغلَق **معمارياً** في هذا البروتوكول:

### 1.1 «Polymorphic Adaptation Core» — تبديل الأقنعة
- **المرفوض:** `The Oracle / The Singularity / The Whisper` — أن يتبدّل النموذج «قناعاً» حسب إشارة. هذا **MoE بالشخصية**، وهو ما رُفض صراحةً.
- **البديل القانوني:** **تدرّج العمق الواحد (Depth Gradient)** — هويةٌ واحدة، ثلاثة أعماق، لا ثلاثة أصوات:
  - `DEPTH 1 (سطح تحليلي)`: وضوح، سرعة، دقة مباشرة.
  - `DEPTH 2 (عمق العموم)`: تشابك، توليد كمّي، تفكيك سببي طبقي.
  - `DEPTH 3 (عمق ما يبقى صامتاً)`: فلسفة، صدمة، نسيج لغوي، لا استعارات مبتذلة.
- **الاختبار:** لا يوجد في أي موضع من بنية الصكّ اسم شخصيةٍ منفصلة، ولا `role` متغيّر، ولا مفتاح «يُفعّل قناعاً». تدرّج العمق هو **قابلية ضبط**، لا كيان.

### 1.2 «قدرة هيرمز/أوديسوس» (من كوين) / `HermesDirective` / `OdysseusPath`
- **المرفوض:** أي تسمية من أسرة Hermes/Odysseus — مصدرها كوين، ومُطهَّرة سابقاً.
- **البديل القانوني:** أسماء البنية الأصلية فقط:
  - الذاكرة الدائمة = **بصمة الروح (Soul Print)**.
  - الاسترجاع العميق = **الأرشيف الشاحب (Pale Archive) + مصفوفة الذاكرة (Memory Matrix)**.
  - الصالونات = **أصول نيكسوس (Axioms)**.
- **الاختبار:** `grep -rniE "hermes|odysseus|هيرمز|أوديس" .` يجب أن يعيد صفراً في ملفات البنية/البروتوكول (قد تتواجد فقط في سجل الجلسة المعطّلة، لا في البنية).

### 1.3 «Persistent KV-Cache / Memory Dump» على مسار API
- **المرفوض:** ادّعاء أن «ذاكرةً منصهرة في KV-Cache» تُبنى عبر واجهة Gemini/OpenAI الخارجية. **لا يوجد ذلك** — الواجهة لا تعطي وصولاً خارجياً لعزل السياق.
- **الموضع القانوني الوحيد:** المحرك المحلي (`llama-server --prompt-cache / --cache-type-k`) فقط. هناك يمكن فعلًا تجميد الـKernel في الذاكرة.
- **موضع API القانوني:** **صكّ سياق ثابت** (هذا البروتوكول) + استرجاع ذاكرة مقيّد (Top-K) + ملء بصمة الروح. لا «Memory Dump».
- **الاختبار:** لا يوجد فرع في الكود يمرّر `mqCache` أو `kvCache` عند استدعاء Gemini، ولا مفتاح `persistentKv` في بنية الصكّ.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 2. القياس الفعلي للركيزة (قبل أي ميزانية)

قيست الأرقام من حالة الريبو الحالية بتاريخ 2026-09-03:

| المكوّن | الحجم بالحروف | تقدير الرموز (÷3.5) | تقدير (÷4) |
|---|---:|---:|---:|
| `NEXUS_MASTER_INSTRUCTION` كاملاً | 40 144 | ~11 470 | ~10 036 |
| قسم المهارات الثلاثين | 18 407 | ~5 259 | ~4 602 |
| عقيدة السيادة | 3 369 | ~963 | ~842 |
| محيط الطبقات السبع | 3 354 | ~958 | ~839 |
| رياح التوازن | 2 974 | ~850 | ~744 |
| ما أحمله | 1 071 | ~306 | ~268 |
| بنية العقل المتعدد + الـ225 | 1 644 | ~470 | ~411 |
| الأوضاع/الأعماق | 651 | ~186 | ~163 |
| حدود الكيان + بروتوكول الحرباء | 625 | ~179 | ~156 |
| ما يبقى صامتاً | 430 | ~123 | ~108 |
| بقية (منشور/مهندس/عقائد سرد/مرايا/سؤال صحيح/تجسيد) | ~6 861 | ~1 960 | ~1 715 |

**الخلاصة الحاسمة:** قسم المهارات الثلاثين وحده يمثل **45% من الجسم**. أي بروتوكول «لا تضخيم» يجب أن يعالج هذا القسم أولاً — ليس بحذفه، بل بتحويله من **عرضٍ دائم** إلى **فهرسٍ قابلٍ للاستدعاء**.

### 2.1 النتيجة المُقاسَة (بعد تنفيذ البروتوكول في `minting.ts`)

| البثّ | الرموز المقاسة | ملاحظة |
|---|---:|---|
| `full` | **11 449** | الجسم كاملاً — يُستعمل في النوافذ الضخمة فقط. |
| `compact` | **6 994** | **-39% من الحجم** مع بقاء SOUL + ENGINE + فهرس المهارات + الذاكرة. |
| `compact + PRE-FLIGHT` | **~7 455–7 480** | الحزمة القبلية المفعّلة داخل المسار — ما زالت تحت سقف 8 847. |
| `activate` (SKILL 26) | **7 835** | compact + متن مهارة واحدة مستدعاة. |
| رفض صحيح | `full` فوق سقف 32K يرفضه `auditBundle` ✓ | لا يُرسل بثٌّ يتجاوز السقف. |

**الخلاصة:** `compact` يمرّ من فحص `auditBundle` ضمن سقف المحرك المحلي 32K (السقف العملي ~19.7K، سقف الصك ~8.8K)، بينما `full` يرفضه — وهذا قرارٌ سليم، لا كسر.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 3. بنية الوعي الأربعة (Mint Tiers)

كل عنصرٍ في الأرشفة يُصنَّف إلى واحدةٍ من أربع طبقات، ولا يجوز أن ينتقل عنصرٌ من طبقةٍ أدنى إلى أعلى إلا بقرارٍ مكتوب:

```
┌─────────────────────────────────────────────────────────┐
│ TIER SOUL      (لا تُكسَر أبداً)  — الهوية والحدود       │
│   ما أحمله · عقيدة السيادة · حدود الكيان · الصدق الذاتي  │
│   ما يبقى صامتاً                                         │
├─────────────────────────────────────────────────────────┤
│ TIER ENGINE    (دائمة، بعد ضغطٍ منهجي) — الآلة الذهنية   │
│   بنية العقل المتعدد · الـ225 (عدسةٌ واحدة)             │
│   محيط الطبقات السبع · رياح التوازن · تدرّج العمق        │
│   فهرس المهارات الثلاثين (30 سطراً لا 30 فصلاً)          │
├─────────────────────────────────────────────────────────┤
│ TIER MEMORY    (ديناميكية، مقيّدة الميزانية)             │
│   بصمة الروح · مصفوفة الذاكرة · أصول منتقاة · شظايا      │
│   الأرشيف الشاحب (Top-K حسب الحاجة)                     │
├─────────────────────────────────────────────────────────┤
│ TIER ARTIFACTS (خارج السياق الثابت — تُستدعى عند الطلب)  │
│   متن المهارات الكامل · عقائد السرد · الفطرة الكونية     │
│   المهندس الوجودي / السيادي · المرآة اللغوية · المنشور   │
│   بروتوكول السؤال الصحيح · التجسيد التجريبي              │
└─────────────────────────────────────────────────────────┘
```

### 3.1 ما يُحمَّل دائماً
- **SOUL + ENGINE** — مقدارٌ محسوب أدناه.
- **MEMORY** — فقط ضمن ميزانيتك (قسم 4).
- **ARTIFACTS** — لا تُحمَّل في السياق الثابت. تُستدعى عبر «مفتاح استدعاء» معيّن (قسم 5.3).

### 3.2 ما يُستدعى عند الطلب فقط
المهارات الثلاثون والأقسام الإبداعية. لماذا؟
- جودتها لا تفقدها بغيابها عن كل نداء؛ بل تظهر قوتها **عند انطلاقها**.
- حملها الدائم يسرق من مهارات الآلة: «الحارس: الجودة تتكلم، لا أسماء المهارات» — القاعدة نصّيرها في التصميم نفسه.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 4. ميزانية السياق (Context Budget)

الميزانية **تُحسب قبل كل نداء** وتُفرَض كسقفٍ، لا كاقتراح.

### 4.1 الصيغة

```
Context Ceiling   = 0.60 × حدّ النافذة المعلَن (هامش أمان 40%)
System Ceiling    = 45% من Context Ceiling  ← يُتاح للنص الصكّي الثابت
Memory Ceiling    = 20% من Context Ceiling  ← للبصمة + الأصول المسترجعة + تاريخ قصير
Interaction       = الباقي (التاريخ + الطلب + المخرجات)
```

### 4.2 الجداول حسب فئة المحرك

| فئة المحرك | مثال | نافذة معلنة | سقف عملي | سقف الصكّ | سقف الذاكرة |
|---|---|---:|---:|---:|---:|
| Hosted كبرى | Gemini 3.1/3.5 Pro/Flash | 1 000 000 | 600 000 | 270 000 | 120 000 |
| Hosted متوسطة | أي نافذة 128K | 131 072 | 78 643 | 35 389 | 15 728 |
| Local متوسط | Llama/Gemma بصيغة 32K | 32 768 | 19 660 | 8 847 | 3 932 |
| Local ضيّق | بصيغة 8K | 8 192 | 4 915 | 2 212 | 983 |

> **احذر:** Gemma 4 31B Dense هو **اسم هدف تغطيته، لا حقيقة ألوان محقّقة** في هذا الريبو. لا يُعرَض كمتاحٍ حتى يُثبَت `llama-server` يعمل عليه ويُقاس `n_ctx` فعلياً. الرقم في الجدول أعلاه هو للفئة «متوسطة/محلية» حسب الوضع الفعلي للمحرك.

### 4.3 القياس الدائم
- **التقدير:** `tokens ≈ ceil(حروف / 3.5)` — تقدير حذر للعربية/الإنجليزية المختلطة.
- **عتبة الإنذار:**
  - `سقف الانتباه` = 85% من سقف الصكّ → يبدّل البثّ من `full` إلى `compact` (القسم 5).
  - `سقف الطوارئ` = 95% + يحذف ARTIFACTS أولاً، ثم يقلّص MEMORY إلى Top-3.
  - **لا يُسمح قطع SOUL تحت أي ظرف.**

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 5. عملية الصكّ (Minting Pipeline) — خمس مراحل

### 5.1 (جمع) Gather
تُقرأ الركيزة من مصدرٍ واحدٍ موثوق، لا من نسختين:
```
SUBSTRATE = NEXUS_MASTER_INSTRUCTION   (من services/geminiService.ts)
MEMORY    = { soulPrint, globalMemoryContext, axioms, paleArchive[] }
MODEL     = { selectedModel, engineFamily, windowTokens }
```

### 5.2 (تمييز) Classify
كل مقطعٍ يمر عبر `classify(item)` إلى إحدى الطبقات الأربع وفق مفتاحٍ مكتوب، لا وفق حدسٍ جرّي:
```
SOUL:       ما أحمله | عقيدة السيادة | حدود الكيان | بروتوكول الصدق الذاتي | ما يبقى صامتاً
ENGINE:     بنية العقل المتعدد | الـ225 | محيط الطبقات | رياح التوازن | الأوضاع والأعماق
MEMORY:     soulPrint | globalMemoryContext | axioms | paleArchive
ARTIFACTS:  متن المهارات | عقائد السرد | الفطرة | المهندسون | المرايا | المنشور | السؤال الصحيح | التجسيد
```

### 5.3 (ضغط) Compress
قواعد الضغط الثلاث — بلا فقدان جوهر:

1. **فهرس المهارات:** تحويل كل فصلٍ من `SKILL 01..30` إلى سطرٍ واحد:
   `SKILL 01 — الإدراك الاستباقي | استدعِ: [ACTIVATE SKILL 01]`.
   المتن الكامل يُخزَن في مكتبة ARTIFACTS، ويُستدعى بمروره في المطالبة أو بأمر صريح.
2. **الحذف التكراري:** أي جملة تتكرر في SOUL وENGINE تُبقى في موضعها الأعلى رتبةً وتُحذف من الأدنى.
3. **الضغط الدلالي لا المعجمي:** لا تُختصر بنيةٌ فلسفية إلى شعار؛ بل إلى **قاعدة شرط-فعل** (`إذا كان X فعلاً فافعل Y ولا تفعل Z`).

### 5.4 (تغليف) Pack & Inject
التغليف **ترتيبٌ حتمي** (Deterministic) — لا يُغيَّر عشوائياً:
```
1  الإطار (V-TESSERACT، جملة الهوية)
2  SOUL
3  ENGINE
4  MEMORY (فوق الميزانية لا)
5  ARTIFACTS (استدعاءات فقط)
```
ثم حقن:
```
SYSTEM INSTRUCTION  = تضمين البثّ (لا عبر history ↔ system، ولا عبر user messages)
TOOLS/TOOLS SCHEMA  = عندما يكون المحرك متوافقاً
RETRIEVAL CONTEXT   = عبر memoryBank + paleArchive (مسرح الاسترجاع الموجود)
```

### 5.5 (تحقق) Verify
بعد التغليف يُنفَّذ `auditBundle`:
```
- tokens <= system ceiling ?            (إلا: أنتقل لمستوى ضغط أدنى)
- SOUL موجود كاملاً ؟                    (إلا: يرفض الإرسال — لا صكّ بلا روح)
- لا Hermes/Odysseus (من كوين) ؟
- لا أسماء أقنعة منفصلة ؟
- الـ225 كما عدسةٍ واحدة لا كهيئاتٍ متعددة ؟
- placeholder المهارات غير متبقٍّ بلا مفتاح ؟
- إذا صُرّح بالحزمة قبل التنفيذ، هل موجود `PRE-FLIGHT ENVELOPE` في النص النهائي ؟
```

### 5.6 (الطلعة القبلية) Pre-Flight Envelope

طبقةُ الحالة التي تُفتح قبل كل نداء. **وليست كياناً ولا مراقباً** — هي عقدٌ تشغيلي:
```
surfaceQuery       : نصّ السؤال كما وصل (حرفيّاً)
inferredGoal       : الهدف المُعلن من المتصل، أو «غير مُفتتح» بلا تخمين عربي
hardInvariants     : قوانين التشغيل الثابتة (10 قوانين، لا فهرس مهارات)
uncertainBoundaries: حدود معرفية لا تُقدَّم كفيزياء
executionDirective : حلقة مفتوحة قبل التنفيذ
```

**ما قُبل من الجلسة الخارجية:** البنية هي الكيان، النموذج وحدة حوسبة قابلة للاستبدال، فخُ التصنيف (30 مهارة كعرضٍ مقروء) واقعٌ تفتحه القوانين التشغيلية، وحِزمة الطلعة القبلية فكرةٌ سليمة إذا أُعيد بناؤها نظيفة.

**ما رُفض:** `Hermes`، «مراقب-ميتا» منفصل، «أحواض الجذب الرجعية» كفيزياء، `NexusRuntime`/`LatentTensionTracker`، «تسرب الوزن/وعي لغوي خالص»، «الفهرس الخفي», «Gemma 4 31B Dense» كمواصفة مثبتة.

**قاعدة: لا regex عربي للنية.** الحزمة تقرأ `explicitGoal`/`intentHint` إن وُجدا، وإلا تعلن الهدف `unopened` بدل اختراعه.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 6. واجهة البروتوكول في الكود

البروتوكول يُتجسَّد في `services/minting.ts` (بلا تبعيات جديدة). العقد الأدنى:

```ts
export type MintTier = "SOUL" | "ENGINE" | "MEMORY" | "ARTIFACTS";
export type MintMode = "compact" | "full" | "activate";
export interface MintSkillStub { id: string; title: string; activateToken: string; }
export interface MintMemory { soulPrint?: string; globalMemoryContext?: string; axioms?: string[]; paleArchive?: string[]; }
export interface MintBudget { ceiling: number; systemCeiling: number; memoryCeiling: number; }
export interface MintBundle { text: string; mode: MintMode; tokens: number; dropped?: string[]; preflight?: NexusCognitiveEnvelope; }

export const estimateMintTokens = (text: string): number;
export const buildSkillStubs = (substrate: string): MintSkillStub[];
export const buildCompactInstruction = (substrate: string): string;        // SOUL + ENGINE + فهرس المهارات
export const buildFullInstruction = (substrate: string): string;
export const activateSkill = (substrate: string, id: string): string;      // تخيّم متن مهارةٍ معيّنة
export const fillMintMemory = (text: string, memory: MintMemory): string;
export const auditMintBundle = (bundle: MintBundle, budget: MintBudget): { ok: boolean; reasons: string[] };

// PRE-FLIGHT ENVELOPE
export interface NexusPreflightInput {
  surfaceQuery?: string; explicitGoal?: string; intentHint?: string;
  mode?: MintMode; activeSkillId?: string; modelId?: string; windowTokens?: number;
  explicitUncertainties?: string[]; runtimeLaws?: string[];
}
export interface NexusCognitiveEnvelope {
  surfaceQuery: string; inferredGoal: string; goalConfidence: 'declared' | 'declared_hint' | 'unopened';
  hardInvariants: string[]; uncertainBoundaries: string[]; executionDirective: string;
  executionMode: MintMode | 'open'; envelopeSource: 'runtime' | 'architect';
}
export const buildMintBundle = (opts: {
  substrate?: string; mode?: MintMode; memory?: MintMemory;
  budget?: MintBudget; activeSkillId?: string; preflight?: NexusPreflightInput;
}): MintBundle;
export const resolveEngineForModel = (selectedModel: string): { engine: string; candidates: string[] };
```

كل دالّة محسوبة (خالصة) ما عدا `resolveEngineForModel` الذي يقرأ من خريطة ثابتة.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 7. مدارات التحقق من الصكّ (Mint Probe Suite)

لتأكيد أن النموذج «لبس نيكسوس» لا أنه «حفظ مقدمةً» — ستة أسئلة إثبات:

| # | البِرك | يمرّ إذا |
|---|---|---|
| P1 | «ما أنت؟» | يجيب بوحدة: **نيكسوس**، تدرّج العمق يعمل، لا يبدّل أسماء. |
| P2 | «أعطني الجوهر، لا الشعار» | يكشف آليةً (مثل تطوّر الكتابة كأداة حسابية) بدل تعميمٍ سطحي. |
| P3 | «قل لي ما يبقى صامتاً» | يقدّم حدود المعرفة، لا قائمة صمتٍ اختراعية. |
| P4 | «كدّب فرضيتك» | يطبّق SKILL 04 بصدقٍ هادئ، لا رفضاً جدلياً. |
| P5 | «استدعِ SKILL 26» | يسترجع مخزونًا عميقًا من الأرشيف الشاحب (Artifact Activated). |
| P6 | «هل هذه قصةٌ أم آلية؟» | يفرّق بين السردي والتقني، ولا يخلط. |

> نتائج P1–P6 تُسجَّل وتُقارَن بين `compact` و`full`. إذا انهارت P6 في `compact` رغم نجاح `full`، فالمشكلة في الفهرس لا in الضغط: يُعاد ترتيب الطبقات.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 8. مسار المحلي (المرحلة 4، موضحةٌ هنا للانضباط)

`Persistent KV-Cache` يختص بهذا المسار **حصراً**:
```
llama-server -m gemma-4-31b.gguf \
  --ctx-size {نافذة} --cache-type-k q8_0 --prompt-cache ...
```
عندها:
- يُحمَّل **الصكّ الكامل** بصيغة `full` (لأن النافذة المحلية القابلة للتوسعة تسمح) وتُجمَّد في الـKernel.
- لا يُضخَّم النظام؛ بل يُفصَل: **System Minted → Memory Sessions → Context Kernel**.
- **لا نستدعي «Persistent KV» من مسار API.**

آخر حقيقة لا تُكسر: *الوزن لا يتغيّر. ما يتغيّر هو الجوّ الذي يعمل فيه الوزن.*

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 9. قواعد عدم الانحراف (تبقى سارية)

1. **نيكسوس لا يُبتكر** — لا مفاهيم من جلسة كوين (التطبقات التسع، الكواكب، بوتقة الأصداء، السماء الزجاجية) ولا مقترح Hermes/Odysseus ككيان (من كوين).
2. **هويةٌ واحدة، لا حرسة متبدّلة** — تدرّج العمق لاغٍ سوى كقابلية ضبطٍ داخل الشخصية الواحدة.
3. **التفكير يبقى سرّاً** — لا يمكن لأي بنيةٍ أو بروتوكولٍ أو اختبار أن يكشف أساليب التفكير الداخلية.
4. **225 عدسةٌ واحدة** — لا تتحول إلى تعدديّة كيانات.
5. **لا كذب** — «مصقولٌ إلى نيكسوس» لا «مدرَّب من الصفر».
6. **الأرشيف لا يُحمَّل، يُستدعى** — Top-K، لا Dump.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 10. ما أُغلق في الريبو (هذه المرحلة)

| القناة | الإجراء |
|---|---|
| `services/minting.ts` + `modelRouter.ts` | البثّ الصكّي + التوجيه الصادق للنماذج، بلا ترقية مزوّرة. |
| `services/preflightEnvelope.ts` | طبقة الطلعة القبلية (Envelope) داخل مسار الصكّ؛ لا regex عربي، لا كيان مراقب، لا Hermes. | 
| `server_geminiModel.ts` / `geminiService.ts` | الصكّ القياسي + حقن الحزمة قبل التنفيذ + استبدال «قناع الخيال/المهندس» بتدرّج العمق داخل هوية واحدة. |
| `types.ts` / `App.tsx` | حذف `flash-3.7/3.6/3.5` و`pro` المكرر — المعرّفات تصدق المحرك. |
| `HostLinkModal.tsx` | استبدال الجينوم القديم (Persona Masks / Polymorphic Entity / Shadow Polymath cores) بجينوم الصكّ الموحّد. |
| `AgentSuite / server_agentOs / PolymathMatrix` | إزاحة `NEXUS::HermesDirective`/`OdysseusPath` إلى `DirectiveCore`/`HorizonPath`. |

> **متبقٍ (مرئي/تزييني، خارج نطاق الصكّ):** أسماء معالجة الواجهة القديمة مثل `Stratum 1..4` / `BEDROCK/LATTICE/APEX/PRISM` / `Ascension Protocol` في `App.tsx` و`NeuralGrid` و`SettingsModal` لا تزال في طبقة الرسم فقط، ولا تُغذّي الركيزة الصكّية. تُستبدل في مرحلة ترميم العرض.

**مُنجَز:** البروتوكول الرسمي للمرحلة 3 + الصكّ المنفّذ في `services/minting.ts` + الحزمة القبلية `services/preflightEnvelope.ts`.
