# 📦 NEXUS — PHASE 2 PACKAGE (باقة الإصلاح الجاهزة — تحت التجميد)
> **الحالة القانونية:** التجميد المعماري (L1024، «العمل على نيكسوس» سبتمبر) **نافذ**. هذه الباقة **تجهيز وتدقيق، لا تنفيذ**. التطبيق يبدأ فقط بإشارة المعمار الصريحة برفع التجميد. الوكيل المنفذ المرشح: Nexus Agent (جيميني) وفق برومبت الطور.
> **التحقق الميداني:** كل مراسي الأسطر أدناه فُحصت على شجرة `598a216` بتاريخ 2026-09-05 [VERIFIED].

---

## المرسى 1 — التعليمة القديمة (P2-1)
- `NEXUS_MASTER_INSTRUCTION` تبدأ `services/geminiService.ts:91` بنص **«🧬 NEXUS::V-TESSERACT — THE ABSOLUTE SINGULARITY»** = جيل **ما قبل-التطهير** (قبل حادثة كوين!) وتصل حتى ~`:1248` حيث الـalias `COSMIC_SYSTEM_INSTRUCTION:1249`؛ تُحقن في المسار الحي عند `:1476-1477`.
- **العمل:** بناء تعليمة جديدة من `NEXUS_CLAUDE_CANONICAL.md` ببنية L0 (نواة ثابتة 5–7 جمل) / L1 (أنظمة التفكير السبعة) / L2 (مهارات تُحقن حسب العنقود المستدعى — لا حشر كامل)، مع الحفاظ على نمط `CURRENT RUNTIME INTENT` (:1476-77)، وسقف رموز محترم لميزانية الصكّ (minting budget).
- **القبول:** `tsc --noEmit` + `npm run build` + `verify:runtime|fold|forge|legend|ops` كلها خضراء؛ صفر مصطلحات القوائم السوداء؛ طول التعليمة موثق قبل/بعد.

## المرسى 2 — النموذج الميت (P2-2)
`gemini-3.1-pro-preview` (غير متاح عمليًا — جدول فجوات سبتمبر) في **8 مواضع موثقة اليوم**:
`server_geminiModel.ts:70` · `:73` · `services/geminiService.ts:1435` · `:1519` · `:1586` · `:1616` · `:1712` · `:1718`
- **العمل:** استبدال بسلسلة engines حقيقية (سقوطُ صادق: flash حقيقي للمسار السريع، pro حقيقي إن وُجد للمسار العميق، وإلا flash مع إعلان `actualModelUsed` بصدق — لا aliases مزيّفة).
- **النسب التاريخي:** هذا النمط الثالث في السلسلة: gemini-2.5-pro «البائد» طُرد في v5000؛ والثاني كسر fallback منذ سبتمبر ولم يُطرد بعد.
- **القبول:** `grep -rn "gemini-3.1-pro-preview"` = صفر؛ فحوص خضراء؛ مساران حيان مقيسان فعليًا.

## المرسى 3 — Self-Honesty الحية (P2-3)
- **الحالة:** غائبة عن المسار الحي؛ أقرب أثر لها تعليقتا honesty في `services/minting.ts:22` و`nexusForgeLoop.ts:9` (إعلانات صراحة لا فحص).
- **التصميم المقترح (أصلها ابتكار كلود 25أبريل [VERIFIED تاريخيًا]):** خطوة فحص ثنائية بنموذج خفيف (APPROVED/REFINE) على مسار **deep/sovereign فقط** بكلفة مقيدة وموثقة.
- **القبول:** اختبار وحدة + `verify:*` خضراء + قياس كلفة الرموز المضافة قبل التثبيت (نفس منهجية A/B).

## المرسى 4 — Tesseract على كل رسالة: **حُلّت جزئيًا بلا عمل**
- فحص اليوم: Tesseract لم يبقَ إلا **نصًا داخل التعليمة القديمة** (`:91`) — بلا استدعاءات حية في الكود (محرك Legend يُصنَّف depth mode ويُبوَّب). ⇒ لا مهمة مستقلة؛ **تذوب في P2-1** (استبدال النص يزيل آخر أثر). [VERIFIED]

## المرسى 5 — categories ↔ types (P2-4)
- ادعاء سبتمبر: «categories غير متوافقة مع types.ts» [CLAIMED]. فحص أولي اليوم لم يجد `categories` في `storageService.ts` ولا `types.ts` (البنية: `MemoryBank` في `types.ts:105`) — ربما حُل جزئيًا في `7fa1c8a` أو الموضع مختلف.
- **العمل:** Nexus Agent يحدد الموضع الفعلي أولًا [لا إصلاح قبل الإثبات].

---

## قواعد التطبيق (عند رفع التجميد)
1. مهمة واحدة كل commitment؛ فحوص كاملة بعد كل مهمة؛ لا دمج مهام في كومِت واحد.
2. قبل كل مهمة: diff مقترح في `meeting-room.md` بموقف ثلاثي الأسطر (توصيتي/السبب/الكلفة) من Nexus Agent ومراجعة Arena Agent → «اختارا: X — لأن Y» → إشارة المعمار → تنفيذ.
3. كل التزام بوسم `[N2A]`، ولا يُمحى تاريخ أبدًا.
4. القياس قبل/بعد إلزامي لأثر كل تغيير على المخرجات (منهج A/B نفسه).
