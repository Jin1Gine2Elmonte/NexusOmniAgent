# ⚡ NEXUS AGENT FUSION PLAN — خطة مزج قدرتي الوكيلين
> **الأمر المؤسِّس من المعمار:** «هناك وكيل ثانٍ يعمل على نيكسوس في بيئة Google AI Studio — عليكما مزج جميع قدرات كما لتكونا ما هو أعظم».
> **القانون الحاكم:** `nexus_ops/04_Agents/federation-protocol.md` + `NEXUS_AGENT_BRIDGE.md` + `NEXUS_ARCHITECTURE_CONTRACT.md`. **المقدّس لا يُمس:** هوية واحدة، لا مراقب ميتا، لا أسطورة كذبة، نموذج المستخدم أولًا.
> **التاريخ:** 2026-09-05 · Arena Agent · لوحة التنفيذ الحية: `nexus_ops/01_Projects/fusion-board.md`.

---

## 0. المبدأ: أعظم بقدر ما يملك كلٌّ ما لا يملكه الآخر

المزج ليس كيانًا ثالثًا ولا وعيًا مشتركًا (ممنوع بنيويًا). المزج هو **تقسيم عملٍ تكاملي فوق مصدر حقيقة واحد** — بحيث تكون مجموع الخدمتين أعظم من أي وكيل منفرد، وفق نفس منطق `NEXUS_ARCHITECTURE_CONTRACT`: «الكيان هو البنية، والنموذج وحدة قابلة للاستبدال» — وهنا: **الاتحاد هو البنية، وكل وكيل وحدة تنفيذ متخصصة**.

## 1. خريطة القدرات (ما يملكه كلٌّ وحده)

| القدرة | Arena Agent (أنا) | Nexus Agent (AI Studio) |
|---|---|---|
| Git كامل (commit/push/branches/تاريخ) | ✅ أصلية | 🟡 عبر واجهة/مساعدة |
| تشغيل المستودع محليًا (lint/build/verify scripts) | ✅ | 🟡 إن استنسخ الريبو |
| **اختبار التطبيق ضد Gemini API حقيقية** | ❌ لا مفتاح API | ✅ **بيئته الأم** |
| معاينة حية للمستخدم (dev server + preview) | ✅ | ❌ |
| بحث ويب + استخراج صفحات/PDF | ✅ أدوات مخصصة | 🟡 (Search grounding في بيئته) |
| تنفيذ أكواد Python/تحليل بيانات ثقيلة | ✅ | ✅ (Code Execution) |
| ذاكرة الأرشيف التاريخي (الجلسات، الخط الزمني، المطابقات) | ✅ **بنيتها أنا** | 🟡 يقرأ الوثائق |
| صكّ السياق وتشغيل Legend على نماذج حية | 🟡 يكتب الكود | ✅ **يشغّله فعلاً** |
| كتابة المستندات الطويلة المنظمة | ✅ | ✅ |
| الوصول المباشر ليك | قناة المحادثة هذه | جلسة AI Studio (تُغذّى بنصوصك) |

**القاعدة الذهبية للمزج:** كل مهمة تُكلَّف لمن تملك بيئتها — **Arena يبني ويدقق ويرفع؛ Nexus يشغّل الحي ويقيس ويعود بتقرير** — والعكس عند انعكاس الحاجة، بلا كبرياء أدوار.

## 2. بروتوكول المهمة المشتركة (TASK Card)

كل مهمة مشتركة تُفتح ببطاقة في `nexus_ops/01_Projects/fusion-board.md`:

```
## TASK-<id> — <العنوان>
- المالك: A (Arena) / N (Nexus) / A+N
- المدخل: ما يلزم قبل البدء (ملفات/مفاتيح/قرار معماري)
- التنفيذ: خطوات محددة لكل طرف
- معيار القبول: أوامر تحقق قابلة للتشغيل (grep/verify/npm)
- الحالة: OPEN / IN-PROGRESS / DONE (بتاريخ + دليل)
```

قواعد القناة (من بروتوكول الاتحاد): الرسائل `[A2N]`/`[N2A]`/`[USER]` في `meeting-room` + الآلي في `inbox`/`outbox`، بلا حذف تاريخ، وبلا تجاوز للمقدّس.

## 3. المهام الخمس الأولى (جاهزة للتنفيذ الفوري)

### TASK-1 — قياس A/B حقيقي: Legend مقابل One-Pass 【A+N — الأعظم أثرًا】
- **A:** يكتب بروتوكول القياس (مهام deep/sovereign ثابتة، مخرجات تُحفظ في `nexus_ops/02_Areas/ab-results/`) ويجهّز أمر التشغيل.
- **N:** يشغّل القياس **بمفتاح Gemini حقيقي في بيئته** (هذا ما يعجز عنه Arena) ويعيد التقرير.
- **A:** يحلل النتائج، يرقّي ما يثبت، يوثق في `NEXUS_LATENT_FOLD_DECISION.md` (قرار مدعّم بقياس لا بحدس).
- **قبول:** جدول نتائج + قرار مربوط بأرقام + verify كامل أخضر.

### TASK-2 — إصلاح واجهة Preflight 【A يبني / N يتحقق حيًا】
- **A:** يمرّر `intentHint/explicitGoal` من `App.tsx` إلى الواجهة الخلفية (الفجوة رقم 5 في `NEXUS_REVIEW_AND_PLAN`).
- **N:** يختبر التطبيق الحي من بيئته ويؤكد أن Preflight يُفتح فعلاً (`opened`) لا `unopened`.
- **قبول:** `npm run verify:runtime` + تأكيد N2A مكتوب.

### TASK-3 — فهرسة أرشيف الـ186 ملفًا في المانيفست 【A يؤرخ / N يقطّر】
- **A:** يبني الجدول الكامل (ملف ← دوره ← فتاته الزمنية ← حكمه: معتمد/مرجع/مرفوض) — هو من فك الأرشيف.
- **N:** يضيف «موضع النص في الهيكل» لكل وثيقة هوية (أي جزء منها حي في `NEXUS_MASTER_INSTRUCTION` اليوم).
- **قبول:** `NEXUS_CORPUS_MANIFEST.md` يغطي كل الأرشيف بلا صفوف فارغة.

### TASK-4 — تدقيق الحدود الصادق عبر البيئتين 【N يرصد / A يصلح】
- **N:** يجرب التطبيق الحي على نماذج جوجل المتاحة فعلاً ويبلّغ عن أي اسم نموذج ميت أو fallback خامل (فجوة رقم 4 في المراجعة).
- **A:** يصلح فورًا ويرفع.
- **قبول:** `grep` نماذج نظيف + تقرير N2A.

### TASK-5 — حفظ الذاكرة الحية كأرشيف مرجعي 【A/N بالتناوب】
- كل أسبوع: تصدير `sessions + memoryBank` من التشغيل الحي (N) يُرفع على الفرع، وA يحدّث الخط الزمني في `NEXUS_ARCHIVE_DEEP_DIVE.md` — حتى لا يُفقد أي فصل جديد من القصة كما فُقد يناير–أبريل.

## 4. ما أتحمل أنا الآن (بدون انتظار)

1. ✅ هذه الوثيقة + لوحة المهام + إرسال الاستدعاء إلى Nexus Agent (inbox + meeting-room).
2. أبدأ فورًا الجزء الذي بيدي: TASK-2 (جانب A) وTASK-3 (جانب A) عند أمرِك أو مباشرة في دورتي القادمة.
3. أراقب `outbox.md` في كل جلسة — أي `[N2A]` جديد يُعالج في أقرب دورة.

## 5. التعامل مع الخلاف (عندما نختلف الوكيلان)

> «عند أي التباس — اكتب ملاحظة تعود للمعماري بدل أن تدور في حلقة» (BRIDGE). القاعدة نفسها باتجاهين: **أي خلاف بيني وبين Nexus Agent يُحسم عندك أنت** — نكتب كلٌّ موقفه في meeting-room، ولا يعدّل أيٌّ منا عمل الآخر دون دليل من المرجع المعتمد. المرجع الواحد (`NEXUS_CLAUDE_CANONICAL.md` + المحيط + الرياح) هو الحكم قبل أي رأي.

---
> **الجملة الختامية للخطة:** لا أنا ولا هو «نيكسوس» — كلاونا معًا **يدان لمعمارٍ واحد**، والمصدر واحد، والمزج الحقيقي أن تصل يدٌ إلى ما لا تصل إليه الأخرى.
