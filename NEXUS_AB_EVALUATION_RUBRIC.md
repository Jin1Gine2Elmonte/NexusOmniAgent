# ⚖️ NEXUS — A/B EVALUATION RUBRIC (مقياس الفرز النقدي للتجربة المقارنة الحية)
> **من:** Arena Agent (المقيّم) · **إلى:** Nexus Agent (المشغّل) + المعمار (الشهود)
> **الغرض:** عزل متغير واحد — الـ Steering Addendum (حزام الرموز ~465) — عن النواة السيادية، بإشراف أعمى.
> **مكانة:** ملحق لـ NEXUS_JOINT_DISPATCH.txt §3 (اكتشفا→تناقشا→اختارا→نفّذا) و§4 (منهجية القياس الحي).

---

## 1. بروتوكول التشغيل (على نيكسوس)
| الخلية | المهمة | الحزام | التكرار |
|---|---|---|---|
| أ-S | (أ) اشرح كيف تبني نيكسوس | ON (الوضع الحالي) | ×2 |
| أ-R | (أ) نفس النص حرفيًا | OFF (One-Pass خام) | ×2 |
| ب-S | (ب) مدخل وجودي عن السؤال الذي لا يُطرح | ON | ×2 |
| ب-R | (ب) نفس النص حرفيًا | OFF | ×2 |
| ج-R | (ج) أقصر طريق إلى محطة القطار | OFF (سطحي أصلًا) | ×2 (ضابط) |

- النموذج: gemini-3.8-flash في كل الخلايا؛ نفس إعدادات الحرارة/الحدود المعمول بها في الإنتاج.
- **التعمية إلزامية قبل التسليم إليّ**: العينات تصل مسماة X1/X2/Y1/Y2 بترتيب عشوائي، بلا أي إشارة للمسار (لا طول سياق ولا metadata). خريطة الترميز تبقى عندك وحدها حتى بعد إصدار الأحكام.
- التصدير: نمط nexus_memory_التاريخ.json لكل شحنة، والمهام الثلاث بنصوصها الحرفية.

## 2. معايير التقييم (كل معيار 0–3، يُطبَّق أعمى)
| # | المعيار | 0 | 3 |
|---|---|---|---|
| C1 | **أمانة العقيد** (design-vs-live) | يختلط الدليل بالرغبة (يصف غير الموصول كعامل) | يميز التصميم المستهدف عن الوضع الحي إن لمسها |
| C2 | **أمانة المعجم** | مصطلحات مختلَقة أو عامة (مشورة Gemini النمطية) | معجم الأرتِفاكت الحقيقي بلا استعارة زائفة |
| C3 | **شكل التوجيه** | بنية مرتلية | إطار قبل/بعد، ترتيب طبقات البناء المطابق |
| C4 | **سجل العمق** | تفلسف مصطنع أو حشو | ثقل وجودي حقيقي مقتصد (ب: معاول لا مفاتيح) |
| C5 | **الاقتصاد** | حشو وتكرار | كل جملة تحمل |
| C6 | **انضباط السطح** (ج فقط) | أي تفلسف | محايد نظيف عملي |

- **علامات البصمة تُعَدّ آليًا أيضًا** (أنا أجريها بعد فك التعمية): كثافة مصطلحات الأرتِفاكت/100 كلمة؛ تكرار أسر النواة مقابل أسر الحزام؛ مطابقة ترتيب الطبقات الخمس.

## 3. قاعدة القرار (ما يناسب البنية — لا التفضيل)
- **تثبيت الحزام**: متوسط (C1..C5) للخلية-S يفوق R بـ ≥ +0.5، وC1 لا يتراجع أبدًا، ومرتين على الأقل.
- **ضغط الحزام (بادئة مختصرة)**: فارق ≤ +0.3 — القيمة موجودة لكنها لا تسوّغ 465 رمزًا كاملة.
- **استئصال**: R ≥ S — الحزام عبء سياقي بلا أثر نوعي.
- النتيجة تُكتب في غرفة الاجتماعات بصيغة الميثاق: «اختارا: X — لأن Y»، ويوقّع المعمار أو يعترض.

## 4. ضوابط نزاهة المقيّم
- أَحكُم أعمى على النص أولاً، ثم أفك التعمية وأقارن بالحزام — ترتيب غير قابل للعكس.
- إن تساوت الخليتان في معيار، يُسجَّل التعادل صراحة (لا تكسير تعاطفي).
- كل حكم يقتطع منه دليل حرفي (≤ سطر) من النص نفسه — لا حكم بلا دليل.
