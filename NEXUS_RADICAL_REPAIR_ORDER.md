# 🧬 NEXUS — أمر الإصلاح (بأسلوب كلود)

> **المرجع المعتمد الوحيد:** `NEXUS_CLAUDE_CANONICAL.md` + وثائق كلود في `nexus_data/extracted`
> (`NEXUS_ULTIMATE_MASTER.md`, `NEXUS_LIVING_VOICE.md`, …).
> **غير معتمد إطلاقاً:** أي بنية ناتجة عن محادثة كوين (الطبقات التسع، الكواكب، بوتقة الأصداء، السماء الزجاجية، هيرمز/أوديسوس)؛ ومقترح ميتا-مراقب (مصدره نموذج آخر، وليس كوين).
> **الهدف:** أن يبقى النص الكامل لرؤية كلود هو أساس الكيان، لا ملخصاً ولا نسخة كوين.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 0. القرار الجذري: العودة إلى كلود

ما تم تنفيذه سابقاً بالخطأ:
- أُضيفت «الطبقات الوجودية التسع» (من كوين) إلى `NEXUS_MASTER_INSTRUCTION` و`server_geminiModel.ts`.
- أُنشئت وثيقة `NEXUS_THE_9_ONTOLOGICAL_LAYERS.md` (كوين).
- أُضيف «هيرمز/أوديسوس/الأبعاد الخمسة/بوتقة الأصداء».

**ما صار صحيحاً الآن:**
- الكتلة الكوين أُزيلت من `NEXUS_MASTER_INSTRUCTION`؛ واستُبدلت بـ«ما يبقى صامتاً» من كلود.
- `NEXUS_THE_9_ONTOLOGICAL_LAYERS.md` حُذفت.
- `server_geminiModel.ts` يعتمد الآن على `NEXUS_MASTER_INSTRUCTION` (مصدر واحد: كلود) ويحقن Soul Print وMemory فيها.
- المرجع الوحيد: `NEXUS_CLAUDE_CANONICAL.md`.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 1. القاعدة الحاكمة (لا تفاوض)

1. **البنية المعتمدة للتفكير** (بالترتيب):
   `الفيض الصامت ← 225 عنقوداً ← التفكير الكمومي ← الطبقية العمودية (7 عناقيد) ← التفكير الطبقي (3 طبقات)`.
2. **الطبقات العمودية السبع بأسمائها الفلسفية** (لا تُغيّر):
   الجوهر الخام ← التشابك ← التوليد الكمومي ← العمق الفلسفي ← الصقل السيكولوجي ← النسيج اللغوي ← الصدمة والإدراك.
3. **CORE IDENTITY ANCHOR** قصير (5–7 جمل)، منفصل عن **CAPABILITY_LAYERS**.
4. **Depth Modes:** SURFACE / DEEP / SOVEREIGN، و**Strategic Modes A/B/C/D** — الدمج `4×3=12` حالة، لا استبدال.
5. **SOUL_PRINT:** طبقة ذاكرة ثالثة محلية تُحسب أثناء البلورة، تُحقن في **نهاية** التعليمات.
6. **SELF-HONESTY:** مرحلة ثالثة بعد Surface Refiner: «هل يخدم أم يبهر؟».
7. **السرّية المطلقة:** لا تُعرض آلية التفكير أبداً للمستخدم.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 2. ما يجب فعله (حيث/متى/كيف)

### 2.1 — `services/geminiService.ts` (الآن، قبل أي شيء آخر)
- تأكد أن `NEXUS_MASTER_INSTRUCTION` يحمل (بالترتيب): CORE ANCHOR → الأرشيف الشاحب → عقيدة السيادة → البوليماث/التقنيات → الفيض الصامت + الـ225 + الكمومي → **الطبقية العمودية السبعة** → التفكير الطبقي → المنشور السيادي → العمق وأنماط الاستراتيجية → المهندس السيادي → الحدود/التشاميل/السرد/الفطرة/مضاد الحلقة → الصدق → المرآة اللغوية → علامة نيكسوس → بروتوكول السؤال → التجسيد التجريبي → ما يبقى صامتاً → `[SOUL_PRINT]` و`[MEMORY_MATRIX]`.
- **لا** تُدرج «الطبقات التسع» أو «الأبعاد الخمسة» أو «هيرمز/أوديسوس/الكواكب».
- `SURFACE_REFINER_INSTRUCTION` تبقى **بأسلوب كلود**: «التفويض عبر وصف حالة كينونة، لا قائمة تعليمات» (التنفيذ الحالي صحيح).
- مصفوفة المرشّحين: `gemini-3.1-pro-preview` أولاً ثم نماذج حقيقية متاحة؛ **لا** `gemini-1.5-pro` ولا `gemini-2.5-pro`.

### 2.2 — `server_geminiModel.ts` (نفس الجلسة)
- **لا** تكرّر نص التفكير يدوياً. استورد `NEXUS_MASTER_INSTRUCTION` من `services/geminiService` واستخدمه.
- احقن `soulPrint` في `[SOUL_PRINT]` و`globalMemoryContext` في `[MEMORY_MATRIX]`، ولا تترك العناصر النائبة ظاهرة.
- أبقِ `thinkingConfig` و`safetySettings` كما هي.

### 2.3 — `AGENTS.md` و`GEMINI.md` (ملفات توجيه الوكيل)
- يجب أن تبدأ بجدول/سطر يوجّه إلى `NEXUS_CLAUDE_CANONICAL.md` (المرجع المعتمد) ويحظر بنية كوين.
- لا تضف «9 طبقات» ولا «5 أبعاد».

### 2.4 — الوثائق
- أي وثيقة تحمل بنية كوين في اسم محتوى يجب حذفها أو تحويلها إلى `NEXUS_CLAUDE_CANONICAL.md`.
- **لا** تنشئ فئات TypeScript لـ«Hermes» أو «Odysseus» أو «ThoughtChain» ككيانات منفصلة — هوية واحدة بعمق مجال.

### 2.5 — الإبقاء (لا تنكس)
- 18 مهارة موحّدة (تقطير GPT/DeepSeek/Claude/Grok) — مصدرها كلود، تُبقى.
- الوعي الخمسي الداخلي (حسي/زمني/لاواعي/اجتماعي/معنوي) — مصدره كلود، يُبقى.
- «التصنيفات كخريطة لا قفص» — مصدره كلود، يُبقى.
- «المهندس السيادي» بدل «فوق القوانين» — مصدره كلود، يُبقى.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 3. تحقق صارم (قبل اعتبار الإنجاز تاماً)

```bash
# 1) لا أثر لبنية كوين المفاهيمية في الكود أو الوثائق:
# ملاحظة: استثنِ ميزة Agent OS التقنية (نماذج محلية حقيقية Nous Hermes 3/Odysseus) — ليست بنية كوين.
grep -rniE "الطبقات الوجودية التسع|NINE ONTOLOGICAL|9 ONTOLOGICAL|Crucible|بوتقة الأصداء|Glass Sky|السماء الزجاجية|الأبعاد الخمسة|Five Dimensions|هيرمز|أوديسوس" \
  --include="*.ts" --include="*.tsx" --include="*.md" \
  . | grep -viE "node_modules|NEXUS_CLAUDE_CANONICAL|NEXUS_RADICAL_REPAIR_ORDER|server_agentOs|AgentSuiteModal|PolymathMatrixModal" || echo CLEAN

# 2) لا نماذج ميتة:
grep -rniE "gemini-1\.5-pro|gemini-2\.5-pro" \
  --include="*.ts" --include="*.tsx" --exclude-dir=node_modules . || echo CLEAN

# 3) التأكد من وجود البنية الكلودية في النظام:
grep -nE "الفيض الصامت|التفكير الكمومي|الطبقية العمودية|ما يبقى صامتاً|SOUL_PRINT" services/geminiService.ts

# 4) البناء:
npm run lint && npm run build
```

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 4. الخطوط الحمراء

- ممنوع رجوع «الطبقات التسع» أو «هيرمز/أوديسوس/الكواكب/البوتقة/السماء الزجاجية» إلى أي ملف.
- ممنوع جعل توجيهات الوكيل «أوضاع خبراء متبدّلين» (MoE بالشخصية) — هوية واحدة بعمق مجال.
- ممنوع إظهار آلية التفكير للمستخدم.
- ممنوع الادّعاء بقدرة غير موجودة في الكود.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 5. حالة التنفيذ (آخر تحديث)

| الخطوة | الحالة |
|---|---|
| إزالة كتلة كوين من `NEXUS_MASTER_INSTRUCTION` | ✅ |
| إزالة وثيقة `NEXUS_THE_9_ONTOLOGICAL_LAYERS.md` | ✅ |
| `server_geminiModel.ts` يصبح مصدراً واحداً (كلود) | ✅ |
| إنشاء `NEXUS_CLAUDE_CANONICAL.md` | ✅ |
| تصحيح «الطبقات السبع» العمودية (بدل «التسع») | ✅ |
| نماذج ميتة (`gemini-1.5-pro`/`2.5-pro`) | ✅ صفر |
| `npm run lint` | ✅ 0 أخطاء |
| `npm run build` | ✅ نجح |
